# 更新日志

本项目使用语义化版本号记录正式发布版本。

## 未发布

## v1.8.0 - 2026-08-26

### 改进

- 预审页和音乐库按实际内容容器宽度响应，修正窄栏裁切、手机端超高按钮、图标错位、嵌套滚动及抽屉焦点管理。
- 新增跟随系统、浅色和深色外观模式，保存浏览器选择并响应系统主题变化。
- 预审归档支持按艺术家、专辑和源路径搜索，并可单独删除不影响音乐文件的归档记录。

### 修复

- 修复逐曲艺术家不同但专辑艺术家一致时被误判为合辑的问题；MusicBrainz 搜索现在固定艺术家条件，并可识别仅有空格或标点差异的发行标题。
- 修复局域网 DNS Fake-IP 导致 NAS 的 Docker 构建无法访问官方 npm/PyPI 域名；构建地址可通过 `NPM_REGISTRY_IP`、`PYPI_ORG_IP` 和 `PYTHONHOSTED_IP` 覆盖。
- 修复 beets 把同名但 MusicBrainz Release ID 不同的发行版误判为重复专辑的问题。
- 兼容部分歌词源输出的非标准 `[mm:ss:cc]` 时间戳，预览、点击跳转和自动跟随按分钟与百分秒正确解析，保存前自动转为标准点号格式。
- 修复音乐库与预审歌词抽屉的深色主题选择器漂移、子工作区会话过期不返回登录页、本地 Flask 找不到 Vite 构建，以及 PWA 外壳缓存版本不随资源变化的问题。

## v1.7.0 - 2026-08-20

### 新增

- 新增可安装的 PWA 外壳、应用图标、Web App Manifest 与 Service Worker；生产镜像会包含并公开必要的 PWA 静态资源。
- 音乐预审新增辅助文件查看与清理能力，并改进歌词搜索及相关操作流程。

### 改进

- 重构移动端音乐库与预审工作区布局，补充紧凑导航、响应式操作区和小屏辅助文件入口。
- 桌面端预审操作区支持自动换行，并约束响应式工作区尺寸，减少窄窗口下的溢出与遮挡。
- Web 服务获得 DSM 回收站所需的 ACL 访问能力，配合现有可恢复清理流程使用。

### 修复

- 修复 PWA 外壳资源在未登录状态或生产镜像中不可用的问题。
- 修复移动端预审辅助文件不可见，以及歌词候选处理中的部分边界行为。

## v1.6.0 - 2026-08-13

- 音乐预审与音乐库歌词编辑器新增 AI 日译中：严格拆分并校验带固定 ID 的歌词行，保留原 LRC 行时间和逐字时间，以同时间戳双语行载入预览，人工确认后才保存。
- 配置页新增 OpenAI 兼容翻译接口、模型、翻译风格、超时及 API Key；默认适配 DeepSeek，API Key 原子写入受限 secret 文件且不回显，也可改为 Ollama 等本地兼容接口。
- 修复旧识别批次仍按文件路径字符串排列，导致第 10–13 轨显示在第 2 轨之前；列表现在始终按当前对应的碟号、轨号实时排序，旧批次缺少置信度时显示“已匹配”而非错误的 `0%`。
- MusicBrainz 多碟发行的曲目对应改为按碟号、轨号显示，并以绿、黄、橙、红区分单曲匹配置信度；手动调整显示为蓝色。
- 对 beets 初次识别后遗留的多余文件和缺失曲目，按标题、艺术家、时长及已有碟轨标签进行发行版内二次配对，减少 Picard 可识别但预审未对应的曲目。
- 音乐预审成功采用歌词、音乐库成功保存并回读歌词后都会自动停止试听并关闭歌词抽屉；保存失败时保留窗口与编辑内容。
- 音乐预审与音乐库打开曲目后会尝试自动播放；无已保存歌词时自动搜索并载入最高分候选，但仍需手工确认保存。
- 音乐库选择歌词候选后会自动切换到试听预览；音乐库与音乐预审的歌词保存操作都提升到三段式导航下方，并在各面板间保持可见。
- 修复音乐预审轮询覆盖已保存歌词状态，以及音乐库等待目录重扫后才显示保存成功的问题。

### 修复

- “按文件名规则入库”使用 `未分类/<专辑艺术家>/<音频标签专辑>/<文件>` 层级，不带入源目录的中间层级；预览、实际入库和任务恢复使用同一目标路径。
- 规则入库会把扫描到的封面、CUE、LOG、TIFF 等非音频附属文件按原相对路径一并移入目标专辑目录。
- 规则入库入口改为与搜索操作一致的白底描边按钮；手工规则列表保存歌词后会与普通曲目一样显示绿色已保存状态。

## v1.5.0 - 2026-08-10

### 新增

- 音乐预审与目标音乐库的歌词编辑器新增 foobar2000 Plus 风格处理：标准化 LRC 时间戳、压缩连续空白行、使用 OpenCC 转为简体，以及按毫秒整体调整行时间和逐字时间。
- 标准格式转换可选择保留增强 LRC 的逐字时间；应用偏移时会合并并移除已有 `[offset:]` 标签，避免播放器重复偏移。

### 改进

- QQ 音乐切换到 Current QRC 搜索与解密链路，支持逐字时间轴和翻译歌词；旧版 LRC 接口保留为兼容回退。
- 网易云音乐切换到 Ex 的 EAPI/YRC 链路，支持逐字时间轴和翻译，并拒绝只有作词作曲信息的占位歌词。
- 酷狗歌词优先下载并解析 KRC 逐字及翻译内容，取不到 KRC 时回退普通 LRC。
- 音乐预审和已入库音乐都可一键内嵌 `[00:05.00]纯音乐，请欣赏`。

### 修复

- 同时间点的原文与翻译会在音乐库同步预览中合并显示，增强 LRC 的逐字时间标记不再混入可见歌词文本。
- 网易云 Current 搜索/读取返回空正文、QQ Current QRC 读取或解密失败、酷狗 KRC 解码失败时，自动降级到对应普通 LRC 链路。
- 音乐库和音乐预审在关闭抽屉、切换页面或刷新浏览器前保护未保存歌词；音乐预审保存成功后才清除未保存状态。
- 拖动歌词滚动条、触摸、滚轮或使用方向键、翻页键查看歌词都会暂停自动跟随，不再被播放进度抢回当前位置。
- CUE 切分会验证已有输出确实是可读取且有时长的音频；数据库虽有完成标记但输出已删除或损坏时会自动重建。

### 验证

- Python：208 项测试及 8 项子测试通过；前端：11 项状态与歌词处理测试、ESLint 和生产构建通过。
- Ruff、Python 与 Node 生产依赖审计、Docker Compose 配置校验均通过。

## v1.4.3 - 2026-08-10

### 改进

- 目标音乐库默认按完整文件夹路径倒序分页，数字开头的手工分类目录不再占据第一页；搜索仍可找到所有分类内容。
- 文件夹内所有曲目都存在内嵌歌词时，在折叠状态直接显示“内嵌完成”。
- 音乐预审歌词抽屉升级为“试听 / 歌词 / 处理”三段式，候选搜索、同步预览、歌词编辑和最终处理决定各自独立。

### 修复

- 历史记录与配置页面的旧管理侧栏补回“音乐库”入口，页面切换后不再丢失导航。
- 音乐预审的同步歌词支持手动滚动暂停跟随，避免播放时抢走用户正在查看的位置。

## v1.4.2 - 2026-08-09

### 改进

- 音乐库编辑抽屉改为“试听 / 歌词 / 标签”三段式切换，播放器保持在顶部，歌词匹配和标签编辑不再堆叠成长页面。
- 歌词搜索、候选读取、歌词保存和标签保存均增加进行中、成功或失败的抽屉内反馈，保存成功明确显示回读校验结果。
- 内嵌歌词与同名 `.lrc` 使用独立编辑缓冲并记住最近保存位置；两种歌词同时存在时可以来回切换，关闭未保存内容前会提示确认。

### 修复

- Web 服务改用有限线程 Gunicorn worker，避免音频响应或慢请求占满单一同步 worker 后阻塞歌词详情、搜索、保存和健康检查。
- 同步歌词不再使用会滚动整页的 `scrollIntoView`，只滚动歌词容器；切到标签或歌词编辑段后，播放不会把抽屉强制拉回歌词位置。
- 修复关闭抽屉或快速切换时旧请求回写当前界面的竞态，并禁止缓存单曲详情，避免保存后重开显示旧歌词。
- 音乐预审的歌词抽屉同步修复滚动范围、异步关闭竞态和遮罩层内操作反馈。

### 验证

- 使用真实 FLAC 与 35 行同步 LRC 验证三段切换、播放后停留标签段、搜索进度、保存回读及关闭重开持久化。

## v1.4.1 - 2026-08-09

### 改进

- 音乐库改为按实际文件夹分页和折叠显示，展开后再查看或编辑单曲，避免大型音乐库铺满曲目列表。
- 文件夹和单曲分别提供可恢复删除；删除文件夹时，音频、同名歌词、封面等附属内容会整体进入隐藏回收区。
- 音乐库侧栏调整为播放器、歌词、音乐标签的顺序，优先展示歌词匹配与同步滚动。
- 音乐库与音乐预审共用浏览器本地音量偏好，关闭侧栏或切换曲目后恢复上次音量和静音状态。

### 安全与可靠性

- 文件夹操作继续限制在目标音乐库内，拒绝根目录、路径穿越和符号链接；文件夹恢复冲突时保留回收记录。
- 文件夹移动和恢复均使用同文件系统原子重命名，清单写入失败会回滚原位置。

### 验证

- Python：195 项测试及 8 项子测试通过；前端：5 项状态测试、ESLint 与生产构建通过。

## v1.4.0 - 2026-08-09

### 新增

- 新增独立“音乐库”页面，原地查询、试听和编辑媒体库中的音频标签，不重复复制文件。
- 支持把手工匹配的同步歌词写入音频内置标签或同名 `.lrc`，并提供按时间轴高亮的歌词预览。
- 新增可恢复删除与回收站界面，音频及同名 `.lrc` 会一起移入目标库内的隐藏回收站。
- 音乐预审新增“按文件名规则入库”，无需 MusicBrainz 曲目对应即可编辑元数据并确认入库，目标固定为 `未分类/<原专辑目录>/<原文件名>`。

### 安全与可靠性

- 音乐库接口统一限制在配置的目标目录内，拒绝目录穿越、符号链接、隐藏回收站和非音频目标。
- 规则入库使用独立 importer、恢复令牌和目标冲突检查；无论全局 `write_tags` 设置如何，均写入并回读校验用户确认的标签。
- 音频试听支持 HTTP Range；删除操作需要显式确认且保留恢复清单。

### 验证

- Python：191 项测试及 8 项子测试通过；真实 FLAC 规则入库、中文 `未分类` 路径、标签回读与幂等重试通过。
- 前端：4 项状态测试、ESLint 与生产构建通过；生产 Docker 镜像构建通过。

## v1.3.0 - 2026-08-09

### 新增

- 音乐预审页新增单曲试听、播放进度条和按时间轴高亮的 LRC 歌词面板。
- 支持从酷狗音乐、QQ 音乐 Ex 和网易云音乐 Ex 手工搜索候选歌词，预览后按单曲保存选择，也可标记纯音乐或跳过。
- beets 入库成功后、清理源目录前，将已确认歌词写入目标音频的内置标签，并读回校验；支持 ID3、MP4 和 Vorbis/FLAC 标签。

### 安全与可靠性

- 试听接口仅允许访问当前预审任务中的音频文件，拒绝目录穿越、符号链接和非音频文件，并支持浏览器 Range 请求。
- 歌词决定持久保存到 SQLite；任务重试时幂等写入标签，标签校验失败则不清理源文件。

### 验证

- Python：179 项测试及 8 项子测试通过。
- 前端：4 项状态测试、ESLint 与生产构建通过。
- 三个在线歌词源和真实 FLAC 标签写入/读回验证通过，生产 Docker 镜像构建通过。

## v1.2.0 - 2026-07-28

### 新增

- Review Worker 支持运行时配置热加载，可动态更新识别并发、自动发现、代理与 beets 入库设置。
- 健康检查支持分别查询 Web、普通 Worker 和 Review Worker，并在启用预审时聚合判断整体状态。
- 音乐预审自动发现补充 `.dff` 与 `.tta` 音频格式支持。

### 改进

- qBittorrent 密码、API Key 和 MusicBrainz 代理密码改为权限受限的独立密钥文件；旧版明文配置会在启动时自动迁移。
- 配置与密钥保存增加跨进程锁、失败回滚和空密码保留，避免服务读取到部分更新的凭据。
- MusicBrainz 连通性诊断复用已配置的认证代理，清空代理后会明确恢复直连。
- 前端按批次和条目隔离异步请求状态，避免旧轮询结果覆盖当前视图或提前解除操作锁定。
- CI 与本地检查流程新增前端状态测试。

### 修复

- 使用 MusicBrainz release track ID 区分同一 recording 在专辑中的多次出现，并兼容无歧义的旧任务数据。
- 阻止同一路径及父子目录创建重叠的活跃预审任务，避免重复识别和入库。
- 修复 copy 模式在文件复制完成但数据库尚未提交时崩溃，重试后无法安全恢复的问题。
- 修复 Windows 下文件时间字段差异导致相同副本恢复失败的问题。
- 保持晚到的 `.cue` 文件只进入整理分轨阶段，不触发已经归档专辑的重复识别。

### 兼容与迁移

- 保持局域网明文 HTTP 部署方式不变。
- 旧版 YAML 中的 qBittorrent 密码、API Key 与代理密码会迁移到 `/app/data/secrets/`；运行账户需要对 `data` 目录具有写权限。

### 验证

- Python：172 项测试及 8 项子测试通过。
- 前端：4 项状态测试、ESLint 与生产构建通过。
- Ruff、Docker Compose 配置、npm audit 与 pip-audit（OSV）通过。

## v1.1.0 - 2026-07-28

### 新增

- 音乐 Inbox 中新增全选、目录展开和音频文件预览，便于批量选择待识别内容。
- 新音乐进入 Inbox 后可自动加入识别流程，并为自动发现与任务归档补充状态管理。
- 支持带账号密码的 HTTP/HTTPS 代理配置，并将配置能力接入 Web 管理界面。
- 新增 Gitea CI、统一的本地检查脚本、Python 开发依赖和前端 ESLint 配置。

### 改进

- beets 入库改为串行执行，增强多碟专辑合并、曲目映射和嵌套目录处理。
- 默认勾选 `.cue` 隔离项，整理完成后不再保留无用 CUE 文件。
- 拆分超大的前端工作区组件与样式，采用自适应轮询降低空闲请求量。
- 改进归档页响应式布局、识别候选选择和任务反馈。
- Compose 服务统一使用同一版本镜像，并增加源码 revision、镜像一致性和健康检查。

### 修复

- 修复已确认曲目在目录移动或重命名后因旧相对路径导致入库失败的问题。
- 修复音乐任务无法产生候选、跨碟曲目被拆分、重复发行匹配和归档展示异常。
- 加强路径边界检查，防止任务路径逃逸或误处理工作目录之外的文件。
- 修复任务并发、重复认领、异常恢复和状态落库中的可靠性问题。

### 验证

- 后端测试、Ruff、前端 lint/build、依赖审计与 Compose 配置检查已纳入统一检查流程。
